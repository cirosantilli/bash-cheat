#!/usr/bin/env bash

#assert statement has 0 return status

#sample calls:
  #assert true
  #assert [ 1 = 1 ]
  #assert [ "a b" = "a b" ]

#if fail, print statement to stderr and exit 1
#else exit 0

if [ ! "$@" ]; then
  echo -e "assertion failed:\n$@" 1>&2
  exit 1
fi
exit 0
